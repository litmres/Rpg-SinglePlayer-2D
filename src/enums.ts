enum gameStateEnum {
	startMenu = 0,
	inGame,
}

enum playerStateEnum {
	movingLeft = 0,
	movingRight,
	movingJump,
	movingFall,
	idle,
}

enum enemyStateEnum {
	movingLeft = 0,
	movingRight,
	movingJump,
	movingFall,
	idle,
}

enum npcStateEnum {
	movingLeft = 0,
	movingRight,
	movingJump,
	movingFall,
	idle,
}

enum levelsEnum {
	level0 = 0,
	level1,
	level2,
	level3,
}
