interface savePlayerInterface{
    //lastCheckPoint: this.player.lastCheckPoint,
    level:number;
    maxhp:number;
    hp:number;
    y:number;
    x:number;
}

interface gateInterface{
    closed:boolean;
    gate:Phaser.Image;
}
